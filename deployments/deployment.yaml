{ apiVersion: apps/v1, kind: Deployment, name: ai-remediator, namespace: ai-remediator, spec: { replicas: 1, selector: { matchLabels: { app: ai-remediator } }, template: { labels: { app: ai-remediator }, spec: { serviceAccountName: ai-remediator, containers: [ { name: agent, image: your-registry/ai-remediator:0.1.0, imagePullPolicy: IfNotPresent, envFrom: [ { configMapRef: { name: ai-remediator-config } } ] } ] } } } }
