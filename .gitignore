# Compiled agent binary (produced by `go build -o agent ./cmd/agent`)
/agent

# Test binaries and coverage output
*.test
coverage.out
*.out

# Go workspace file (machine-local)
go.work
go.work.sum

# OS / editor cruft
.DS_Store
*.swp
*~
